name: models
description: Shared data models for LocalGrocery apps
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Serialization
  json_annotation: ^4.8.1
  equatable: ^2.0.5
  
  # Core package
  core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  flutter_lints: ^3.0.1
